# Blackbox
Blackbox is a Turing-complete assembly-like language with an assembler and a bytecode interpreter. It uses Intel assembly syntax. (register order is dst, src)
### Docs
See [docs.md](docs/docs.md) and [examples.md](docs/examples.md)
### Building
Run `make` in the project directory on a Linux or macOS host.
